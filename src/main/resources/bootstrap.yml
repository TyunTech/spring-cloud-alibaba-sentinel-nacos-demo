spring:
  application:
    name: micro-service
  profiles:
    active: dev
  cloud:
    nacos:
      config: 
        file-extension: yml
        server-addr: 127.0.0.1:8848
        username: nacos
        password: nacos
